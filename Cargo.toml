[package]
name = "plato-prediction"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Prediction encoding for Z_out side of Dual-DB JEPA"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4", "serde"] }
